name: test

on:
  workflow_dispatch:
    inputs:
      env:
        type: choice
        description: Choice Environment to Deploy
        required: true
        default: 'prod'
        options:
          - 'prod'
          - 'staging'

jobs:
  deployment:
    runs-on: ubuntu-latest
    environment:
      name: ${{ inputs.env }}
      url: https://${{ inputs.env }}
    steps:
      - uses: actions/checkout@v2
      - name: test
        run: echo ${{ inputs.env }}
